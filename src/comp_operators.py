# comparison operators
# ==, !=, <, >, <=, =>, <> (inequality) obsolete
areEqual = a == b
areNotEqual = a!= b   # a &lt;&gt; b works, but obsolete
isBigger = a > b
isLess = a < b
biggerOrEqual = a >= b
lessOrEqual = a <= b
